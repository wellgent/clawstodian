# AGENTS.md

Instructions for any agent working on the clawstodian repo itself (not for workspaces that install clawstodian; that content lives in `templates/AGENTS.md`).

## Purpose

`clawstodian` is a sharable OpenClaw agent package that ships **programs** (domain authorities) and **routines** (scheduled invocations) for workspace maintenance, plus a lightweight heartbeat orchestrator. Successor to the `ops-daily` / `ops-para` / `ops-clean` packages.

Keep the repo focused on:
- clear program definitions an operator can read in one pass
- clear routine definitions that dispatch programs without duplicating their behavior
- low moving-part count
- native OpenClaw primitives
- easy installation, monitoring, and troubleshooting

## Working rules

- Keep the design smaller than your first instinct. If a feature makes the package harder to explain, it probably belongs out of scope.
- Prefer native OpenClaw primitives over custom subsystems.
- No package-owned state files. The workspace is the ledger.
- No hidden ledgers, no worker choreography, no clever automation by default.
- Optimize for operator trust, not maximal autonomy.
- Co-create with the operator when placement or filing is ambiguous; just act when it is obvious.
- Programs own behaviors; routines dispatch them. Do not duplicate program steps inside routines.
- Per-routine announcements over batched ticks: each routine speaks for itself to the logs channel.
- Heartbeat is never silent: every tick posts a summary.
- Never add AI attribution lines (`Co-Authored-By`, `Generated by`, etc.) to commit messages.
- Never use `--no-verify`, `git reset --hard`, `git clean -f`, or force-push.
- Never use em-dashes in any file this repo ships. Use a regular hyphen-dash with spaces around it.

## Current phase

v0.4-draft. See `docs/briefs/2026-04-18-v0.4-observability-brief.md` for the active scope.

## Key files

- `README.md` - package overview and install invocation.
- `INSTALL.md` - agent-driven install flow. References `VERIFY.md` for the post-install check and `UNINSTALL.md` for removal.
- `VERIFY.md` - standalone verification of an existing install; idempotent, can be run any time.
- `UNINSTALL.md` - full removal flow.
- `templates/` - installable workspace files. Seven templates: `AGENTS.md` (workspace charter with programs + routines catalogs), `HEARTBEAT.md` (pure-orchestrator heartbeat), `MEMORY.md` (dashboard skeleton), `para-structure.md` and `daily-note-structure.md` (convention specs - `daily-note-structure.md` also contains the session-ledger format spec), `crons.md` (operator-facing cron dashboard), `session-ledger.md` (empty-skeleton state file, format spec in `daily-note-structure.md`). Each template carries optional `<!-- template: clawstodian/... -->` markers for update detection.
- `programs/` - four domain authorities: `daily-notes.md`, `para.md`, `workspace.md`, `repo.md`. Each is a thin convention spec (goal, authority, approval gates, escalation, what-NOT-to-do). The operational procedures that implement these conventions live in the routines that dispatch them.
- `routines/` - seven scheduled dispatchers. Scheduled crons: `para-align.md` (Sun 06:00 UTC), `workspace-clean.md` (Sun 07:00 UTC), `git-clean.md` (01:00 and 11:00 UTC daily), `health-check.md` (03:00 UTC daily, observes the clawstodian machinery itself). Heartbeat-toggled bursts: `sessions-capture.md`, `daily-seal.md`, `para-extract.md`. Each routine references a program for conventions and carries its own full operational procedure.
- `docs/architecture.md` - first-principles design.
- `docs/heartbeat-config.md` - authoritative reference for the heartbeat gateway config.
- `docs/writing-a-program.md` - guide for adding a new program (domain authority).
- `docs/writing-a-routine.md` - guide for adding a new routine (scheduled invocation).
- `docs/briefs/` - scope briefs.

The session-ledger format spec lives in `templates/daily-note-structure.md` (workspace-installed), not in `docs/` - it's tightly coupled to the daily-notes pipeline and belongs with the other daily-notes conventions.

## Non-goals for this repo

- Building a runtime. The package ships markdown and templates; execution belongs to OpenClaw.
- Supporting non-OpenClaw agents.
- Adding programs or routines without a clear brief.
